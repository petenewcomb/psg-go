name: Release

permissions:
    contents: write # to create release

on: workflow_dispatch

jobs:
  lint:
    uses: petenewcomb/ci-go/.github/workflows/release.yml@main
    with:
      publishDocs: true
